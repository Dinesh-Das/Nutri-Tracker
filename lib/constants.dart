import 'package:flutter/material.dart';

const Color login_bg =  Color(0xf000FFFF);
const Color signup_bg = Color(0xFFBBAAff);

const double defpaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 350);
